'use client';

import { ContactSection } from '@/components';

export default function ContactPage() {
    return (
        <main className="pt-20">
            <ContactSection />
        </main>
    );
}
